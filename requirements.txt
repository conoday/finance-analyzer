# Core API / Lambda requirements — keep this lean (< 500 MB ephemeral storage).
# For local Streamlit dev, use:  pip install -r requirements-local.txt
pandas
numpy
scikit-learn
openpyxl
fastapi
uvicorn[standard]
python-multipart
# Supabase integration
supabase
PyJWT[crypto]
# HTTP client (used by Telegram bot to call Telegram API)
httpx
